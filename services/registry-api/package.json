{
  "name": "@resourcexjs/registry-api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply resourcex-registry --local",
    "db:migrate:remote": "wrangler d1 migrations apply resourcex-registry --remote"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.1",
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "drizzle-kit": "^0.31.8",
    "typescript": "^5.9.3",
    "wrangler": "^4.0.0"
  }
}
